pluginManagement {
    repositories {
        maven(url = uri("https://packages.jetbrains.team/maven/p/reflekt/reflekt"))
        maven(url = uri("https://plugins.gradle.org/m2/"))
    }
}

rootProject.name = "mathwiki"
